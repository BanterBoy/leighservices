{
  "name": "leighservices",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for leigh-services.com (Tailwind + Sass). Used by CI only — no runtime dependencies on the static site.",
  "scripts": {
    "build:tailwind": "tailwindcss -i ./assets/css/tailwind.src.css -o ./assets/css/tailwind.css --minify",
    "build:sass": "sass assets/sass/main.scss assets/css/main.css --style=compressed --no-source-map",
    "build": "npm run build:sass && npm run build:tailwind"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.7",
    "sass": "^1.77.0",
    "tailwindcss": "^3.4.4"
  }
}
